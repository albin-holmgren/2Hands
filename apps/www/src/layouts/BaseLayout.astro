---
import "../styles/global.css";
import { getMarkdownAlternate } from "../agent-content";
import {
  DEFAULT_LOCALE,
  LOCALE_HTML_LANG,
  LOCALE_OG,
  isLocale,
  localeHomePath,
  type Locale,
} from "../i18n/locales";
import { SITE_NAME, SITE_URL } from "../site";

type Props = {
  title?: string;
  description: string;
  structuredData?: object;
  locale?: Locale;
  skipLabel?: string;
  homeAlternates?: boolean;
};

const {
  title = SITE_NAME,
  description,
  structuredData,
  skipLabel = "Skip to content",
  homeAlternates = false,
} = Astro.props;

const locale: Locale = isLocale(Astro.currentLocale)
  ? Astro.currentLocale
  : (Astro.props.locale ?? DEFAULT_LOCALE);

const canonical = new URL(Astro.url.pathname, SITE_URL).toString();
const structuredDataJson = structuredData
  ? JSON.stringify(structuredData).replaceAll("<", "\\u003c")
  : null;
const markdownAlternate = getMarkdownAlternate(Astro.url.pathname);
const htmlLang = LOCALE_HTML_LANG[locale];
const ogLocale = LOCALE_OG[locale];
const homeUrls = {
  en: new URL(localeHomePath("en"), SITE_URL).toString(),
  de: new URL(localeHomePath("de"), SITE_URL).toString(),
  ko: new URL(localeHomePath("ko"), SITE_URL).toString(),
};
---

<!doctype html>
<html lang={htmlLang}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <meta name="robots" content="index, follow, max-image-preview:large" />
    <meta name="theme-color" content="#FDFDFD" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content={SITE_NAME} />
    <meta property="og:locale" content={ogLocale} />
    {locale !== "en" && <meta property="og:locale:alternate" content={LOCALE_OG.en} />}
    {locale !== "de" && <meta property="og:locale:alternate" content={LOCALE_OG.de} />}
    {locale !== "ko" && <meta property="og:locale:alternate" content={LOCALE_OG.ko} />}
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <link rel="canonical" href={canonical} />
    {
      homeAlternates && (
        <>
          <link rel="alternate" hreflang="en" href={homeUrls.en} />
          <link rel="alternate" hreflang="de" href={homeUrls.de} />
          <link rel="alternate" hreflang="ko" href={homeUrls.ko} />
          <link rel="alternate" hreflang="x-default" href={homeUrls.en} />
        </>
      )
    }
    {markdownAlternate && (
      <link rel="alternate" type="text/markdown" href={markdownAlternate} />
    )}
    <link rel="sitemap" href="/sitemap-index.xml" />
    <link rel="icon" href="/favicon.ico" sizes="any" />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,400..700;1,9..40,400..700&family=Newsreader:opsz,wght@6..72,400..700&display=swap"
      rel="stylesheet"
    />
    {structuredDataJson && <script is:inline type="application/ld+json" set:html={structuredDataJson} />}
    <title>{title}</title>
    <script>
      import "../analytics";
    </script>
  </head>
  <body>
    <a class="skip-link" href="#main">{skipLabel}</a>
    <div class="page-shell">
      <slot />
    </div>
  </body>
</html>
